(* exempl*e de com(*mentai*re **)

program p
    begin
        write (5 + 9 * 2 + 6 / 2);
        write "\n";
        write (5 ^ 2 * 2 + 6 / 2);
        write "\n";
        write (5 + 2 * 2 + 6 ^ 2);
        write "\n";
        write (5 - (2 * 2 + 6 ^ 2));
        write "\n";
    end
